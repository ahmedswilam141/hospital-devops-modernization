apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: hospital
  labels:
    app: redis
spec:
  type: ClusterIP
  clusterIP: None   # Headless service — StatefulSet pods get stable DNS names
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
